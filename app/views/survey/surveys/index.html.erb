<%= title t(:listing_surveys) %> <%= link_to t(:new_survey), new_object_path %>

<ul class="listing link">
  <%- @surveys.each do |survey| %>
    <li><%= link_to survey.title, object_url(survey) %>
  <% end %>
</ul>

