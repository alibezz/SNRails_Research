<%= title t(:new_survey) %>

<%= error_messages_for :survey %>

<% form_for(:survey, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t(:create) %>
  </p>
<% end %>
