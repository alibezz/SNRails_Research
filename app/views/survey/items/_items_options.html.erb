<% unless @survey.is_active or current_user.is_collaborator?(@survey) %>
    <%= link_to t(:add_question), new_survey_survey_item_path(@survey, :item_type => "question") %>
    <%= link_to t(:add_section), new_survey_survey_item_path(@survey, :item_type => "section") %> 
<% end %>
