<h1><% t(:new_item) %></h1>

<%= error_messages_for :item %>

<% form_for(:item, :url => collection_url) do |f| %>
  <%= render :partial => "#{@item_type}_form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to t(:back), collection_url %>
