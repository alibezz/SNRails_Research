<h1><%= t(:new_item_value) %></h1>

<%= error_messages_for :item_value %>

<% form_for(:item_value, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t(:create) %>
  </p>
<% end %>
<br/>
<%= link_to t(:back), collection_url %>
