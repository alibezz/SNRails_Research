<h2><%= t(:edit_item_value) %></h2>

<%= error_messages_for :item_value %>

<% form_for([:admin, @item, @item_value]) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t(:update)%>
  </p>
<% end %>
