<h1>New Questionnaire</h1>

<%= error_messages_for :questionnaire %>

<% form_for(:questionnaire, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to 'Back', collection_url %>